# App page tests

Any file in this directory is considered a page component unit test. The tests
live in this directory because NextJS will build any `.tsx` file in the `app`
directory as a route, so the tests need to be somewhere else.